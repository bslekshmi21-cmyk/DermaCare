name: dermatologist_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  bottom_nav_layout: ^3.0.10
  file_picker: ^11.0.0
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
